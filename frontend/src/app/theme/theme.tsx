import { createTheme } from '@mui/material';

const theme = createTheme({
  typography: {
    fontFamily: 'Inter',
  },
  palette: {
    secondary: {
      main: '#252525',
    },
  },
});

export default theme;
